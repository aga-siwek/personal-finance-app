[project]
name = "personal-finance-backend"
description = "Flask REST API for the Personal Finance App (Frontend Mentor challenge + receipt scanner)"
version = "0.1.0"
requires-python = ">=3.9"

[project.optional-dependencies]
dev = [
    "pytest",
]
