# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (never commit secrets; .env.example is the committed template)
.env
.env.*
!.env.example

# QA artifacts — screenshots/raw data are regenerable via the /manual-tester
# skill, so only the written report.md is tracked.
docs/qa-reports/**/*.png
docs/qa-reports/**/raw-results.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
